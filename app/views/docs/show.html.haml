%h1= @doc.title
%p= @doc.content

= link_to "All Documents", docs_path
= link_to "Update Document", edit_doc_path(@doc)
= link_to "Trash It", doc_path(@doc), data: { turbo_method: :delete, turbo_confirm: "Are you sure?" }